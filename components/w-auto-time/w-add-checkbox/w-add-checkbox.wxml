<!--components/w-auto-time/w-add-cehckbox/w-add-checkbox.wxml-->
<view>
<checkbox-group bindchange="checkboxChange">
  <label class="checkbox" wx:for="{{items}}"wx:key="key" >
    <checkbox value="{{item.name}}" checked="{{item.checked}}"/>{{item.value}}
  </label>
</checkbox-group>
</view>